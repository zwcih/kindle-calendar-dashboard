# Private deployment data; the example stays visible.
config*.conf
!config.example.conf
wifi-ssid.conf
*.local.*
image-auth*.conf
!image-auth.example.conf
request.conf
calendar-image-auth.*/
.env*

# Images, logs, captures and same-filesystem worker temporary files.
*.png
*.log
*.err
*.tmp
*.lock
.calendar-*/
.calendar-auto-refresh.*/
.dedicated-log.*
*.bak
*.backup
*~
*.orig
*.rej
backups/
cache/
runtime/
